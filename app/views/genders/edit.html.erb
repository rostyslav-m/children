<h1>Edit genders</h1>

<%= form_for :gender, url: gender_path(@gender), method: :patch do |f| %>

    <% if @gender.errors.any? %>
        <dif id="error_explanation">
          <h2>
            <%= pluralize(@gender.errors.count, "error") %> prohibited
            this gender from being saved:
          </h2>
          <ul>
            <% @gender.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
          </dif>
    <% end %>

    <p>
      <%= f.label :name_gender %><br>
      <%= f.text_field :name_gender %>
    </p>


    <p>
      <%= f.submit %>
    </p>
<% end %>

<br/>
<%= link_to 'Back', genders_path %>

